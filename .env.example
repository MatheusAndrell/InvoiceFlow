# InvoiceFlow - Environment Example
# Copie para .env e ajuste se necessário.

# Banco (usado no docker-compose para montar DATABASE_URL)
POSTGRES_USER=invoiceflow
POSTGRES_PASSWORD=invoiceflow
POSTGRES_DB=invoiceflow

# API / Auth / Criptografia
JWT_SECRET=change_me_super_secret
ENCRYPTION_SECRET=change_me_32_chars_or_more

# Mock da prefeitura (0.0 a 1.0)
PREFEITURA_SUCCESS_RATE=0.8

# Webhook opcional (deixe vazio para não enviar)
WEBHOOK_URL=

# Frontend build-time (opcional fora do Docker)
# VITE_API_URL=http://localhost:3000
