# Database
POSTGRES_USER=invoiceflow
POSTGRES_PASSWORD=invoiceflow_secret
POSTGRES_DB=invoiceflow

# JWT (use a long, random string)
JWT_SECRET=change_me_to_a_long_random_secret_at_least_32_chars

# Encryption (used for AES-256-CBC certificate password encryption)
ENCRYPTION_SECRET=change_me_to_a_long_random_secret_at_least_32_chars

# Prefeitura Mock configuration (0.0 to 1.0 success rate)
PREFEITURA_SUCCESS_RATE=0.8

# Optional: Webhook URL to call on successful NFS-e issuance
WEBHOOK_URL=
